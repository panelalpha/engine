# yaml-language-server: $schema=../../platforms/_schema.json
id: remix
label: Remix
priority: 905
runtime: node
port: 3000
output_directory: build
requires:
  node: runtime
detect:
  any:
    - glob: remix.config
    - dep:
        - '@remix-run/node'
        - '@remix-run/react'
        - '@remix-run/dev'
commands:
  - id: npm-install
    stage: build
    role: dependencies
    run: '{{js.install}}'
    description: "Resolved from the project's lockfile — npm ci, pnpm install --frozen-lockfile, yarn or bun."
  - id: remix-build
    stage: build
    run: '{{js.build:npx remix build}}'
    description: "The project's own build script wins over this default when package.json defines one."
  - id: serve
    stage: start
    serve: true
    run: '{{js.start:npx remix-serve ./build/server/index.js}}'
    description: "The project's own start script wins over this default when package.json defines one."
