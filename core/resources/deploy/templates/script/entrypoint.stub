#!/bin/sh
# Generated by PanelAlpha from {{ source }} — do not edit.
# Platform: {{ platform }}
set -e

PA_PHASE="${{{ phase_env }}:-{{ default_phase }}}"

pa_step() { printf '[panelalpha] %s: %s\n' "$1" "$2" >&2; }
pa_skip() { printf '[panelalpha] %s: %s failed (optional, continuing)\n' "$1" "$2" >&2; }

{{ stage_blocks }}
{{ start_commands }}
{{ serve }}
